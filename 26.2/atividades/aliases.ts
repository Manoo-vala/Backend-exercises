type bird = {
  name: string,
  age: number,
  fly: boolean,
}

type Sum = (x: number, y: number) => number

type address = {
  street: string,
  house: number,
  district: string,
  city: string,
  national: boolean,
}